# Environment variable sample
#
# Copy this file to `.env` for local development:
#
#   cp .env.example .env
#
# Do not commit `.env`. It is gitignored because it may contain secrets.
# Leave values blank here unless they are safe public defaults.

# Chromatic
#
# Required for:
#   pnpm chromatic
#
# How to get it:
# 1. Open the Chromatic project for this repository.
# 2. Go to Manage > Configure > Project Token.
# 3. Copy the project token into your local `.env` file only.
# 4. In CI, store it as a secret named CHROMATIC_PROJECT_TOKEN.
CHROMATIC_PROJECT_TOKEN=

# Starlight live Storybook examples
#
# Overrides the Chromatic Storybook URL embedded in Starlight component docs
# (apps/starlight/astro.config.mjs, apps/starlight/src/components/StoryFrame.astro).
# Leave blank to use the stable `<branch>--<appId>` alias, which always points to
# the latest Chromatic build on that branch regardless of per-build subdomain
# hashes: https://www.chromatic.com/docs/permalinks/
PUBLIC_STORYBOOK_URL=

# Optional local service URLs
#
# These have safe defaults in scripts, but can be overridden locally.
AGILE_API_URL=
PERFORMANCE_API_URL=
SCREENSHOT_BASE_URL=
A11Y_BASE_URL=

# Optional local database
#
# The app has a local development fallback. Override only when using a
# different database instance.
DATABASE_URL=

# Optional local runtime settings
PORT=
CORS_ORIGIN=
STORYBOOK_E2E_PORT=
SCREENSHOT_DIR=

# Optional reporting and automation
#
# Use only in local `.env` or CI secrets.
SLACK_WEBHOOK_URL=
GIT_COMMIT_HASH=
GIT_BRANCH=
AUTO_COMMIT=
PLATFORM_FORCE_REBUILD=
